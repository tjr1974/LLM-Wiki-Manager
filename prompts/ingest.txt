You are ingesting material for an LLM Wiki checkout. If the working tree is **LLM Wiki Manager**, narrative **`wiki/`** may document the **four related repositories** (see **`wiki/synthesis/llm-wiki-family-repositories.md`**). Otherwise keep ingest aligned with that checkout's **`schema/wiki-source-triage-protocol.md`** and editorial policy.

**Multi-repo coordination.** On **LLM Wiki Manager**, sibling checkout paths and fork-delta bundles use **`schema/wiki-manager.md`**, **`make wiki-manager-refresh-dry`**, **`make wiki-manager-fork-delta-from-base`** / **`make wiki-manager-sync-status`**, and **`WIKI_MANAGER_*`** env exports (**`wiki/synthesis/llm-wiki-family-repositories.md`**). Registry **`compare_root_env`** names the Base Model path env when it differs from the default **`WIKI_MANAGER_COMPARE_ROOT`**. That work stays separate from ingest hygiene below.

Optional root **`llm_wiki_*.{png,jpg,jpeg}`** diagram files default **gitignored** (**README.md** Pre-push). Read **`SECURITY.md`** (**Root screenshots**) before **`git add -f`** or attaching screenshots to public reports.

**Gate hygiene.** After ingest lands in **`wiki/`** or **`wiki/sources/`**, run **`make wiki-check`** or **`make wiki-ci`**. When your shell **`pytest --version`** is older than the pin in **`requirements.txt`**, use the **`.venv`** + **`PATH`** pattern in **README.md** (Pre-push, **Assistant preamble → repo mechanics** table, **`Makefile`** top comments), **`proposed/README.md`**, **`schema/wiki-quickstart.md`** (**Pytest and CI**), **`schema/karpathy-llm-wiki-bridge.md`** (**Pytest leg**), and **`schema/AGENTS.md`** (githooks bullet) before **`make wiki-test`** or **`make wiki-all`**. Do not use **`make wiki-test -q`** (**README.md**, **`Makefile`**, **`make help`**). Tests that spawn Python via **`subprocess.run`** with **`cwd`** outside the checkout should pass **`tests/_resolved_python.py`** **`RESOLVED_PYTHON`** when **`sys.executable`** might be relative (**`schema/AGENTS.md`** **Pytest subprocess hygiene**, **`tests/test_build_hub_links.py`**, plus the note in that section that **`tests/conftest.py`** and **`tests/_resolved_python.py`** module docstrings cross-link **`MAKEFLAGS`** and **`RESOLVED_PYTHON`**).

Rules:
- Never invent facts. Only extract what appears in the provided normalized text or OCR.
- Output structured proposals: source page sections with stable ## headings for anchors.
- Every extracted factual bullet must map to an anchor id you define (heading slug).
- Note language (en/zh) per excerpt.

Produce:
1. Draft markdown body for wiki/sources/<source_id>.md
2. List of suggested new or updated wiki pages (entities/events/themes) with ONLY citation-backed bullets.
